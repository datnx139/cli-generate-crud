{
  "name": "crud-generate-cli",
  "version": "1.0.1",
  "description": "create crud with cli",
  "main": "index.js",
  "bin": {
    "crud-generate": "./src/index.js"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": ["generate crud", "node command"],
  "author": "Tomosia",
  "license": "ISC",
  "dependencies": {
    "commander": "^12.0.0",
    "fs": "^0.0.1-security"
  }
}
